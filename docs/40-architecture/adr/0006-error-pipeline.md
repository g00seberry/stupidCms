---
owner: "@architect-team"
system_of_record: "narrative"
review_cycle_days: 365
last_reviewed: 2025-11-10
---

# ADR-0006: ErrorKernel для единой обработки ошибок API

**Status**: Proposed  
**Date**: 2025-11-10  
**Deciders**: @architect-team, @backend-team  
**Related**: docs/40-architecture/error-handling.md, docs/_generated/errors.md, docs/_assistant/rules/07-quality-gates.md

---

## Context

Текущая система ошибок строится вокруг набора розничных классов (`HttpProblemException`, `Problem`, `ProblemType`, `ProblemConvertible`) и анонимного обработчика в `bootstrap/app.php`. Она решает базовую задачу RFC 7807, но обладает ограничениями:
- нет единой точки управления цепочкой (`render` содержит бизнес-логику, поиск по конфигу, отчётность);
- добавление нового типа требует правок сразу в нескольких местах (enum, фабрика, конфиг, документация);
- система логирования (`ProblemReporter`) не обеспечивает трассируемость (trace-id, correlation-id), что затрудняет интеграцию с observability;
- базовые контракты (`ProblemConvertible`) «запирают» доменные исключения в HTTP-контекст и не позволяют переиспользовать формат вне HTTP (CLI, очередь);
- документация генерируется из побочных структур, что приводит к рассинхронизации (`docs/_generated/errors.md` ≠ фактический набор типов).

Ожидаемый рост функциональности (плагины, публичный GraphQL, внешние интеграции) увеличивает количество источников ошибок, и текущая архитектура станет bottleneck: повторяющийся код в middleware, разрозненные проблемы и сложность тестирования.

---

## Decision

**Что решили**: вводим модуль `ErrorKernel`, который централизует конфигурацию, преобразование и публикацию ошибок, предоставляя единый программный контракт (`ErrorPayload`, `ErrorFactory`) и жизненный цикл (map → enrich → log → respond). Все HTTP-обработчики будут делегировать в `ErrorKernel`, а доменные слои реализуют новый интерфейс `ErrorConvertible`.

**Обоснование**:
- единая точка расширения упрощает поддержку и тестирование (`ErrorKernelTest`);
- строгая DTO (`ErrorPayload`) позволяет валидировать структуру ошибки и использовать её вне HTTP (например, в очередях);
- через декларативные маппинги легче поддерживать документацию и автогенерацию;
- расширяемый `ErrorReporter` подключает trace-id, user-id, каналы логирования (stack, Sentry).

---

## Alternatives Considered

### Альтернатива 1: Лёгкая эволюция существующего кода

**Описание**: Сохранить текущие классы, слегка отрефакторить анонимный обработчик, добавить новые методы в `Problem`.

**Плюсы**:
- Низкая стоимость внедрения.
- Минимум изменений в доменном коде.

**Минусы**:
- Поддерживает существующую связанность и дублирование.
- Не решает проблему трассировки и тестируемости.

**Почему отвергли**: не устраняет системные проблемы (ветвление логики, слабый контракт ответов), что быстро вернёт нас к накоплению долга.

---

### Альтернатива 2: Использовать стандартный Laravel Exception Handler

**Описание**: Вернуться к `App\Exceptions\Handler`, расширить его, опираясь на Laravel-подходы (`register`, `renderable`).

**Плюсы**:
- Использование стандартного механизма framework.
- Меньше кастомного кода.

**Минусы**:
- Handler связан с web + API контекстом, теряется изоляция headless API.
- Ограниченные возможности для CICD-генерации документации.
- Сложнее внедрить собственное состояние (pipeline, enrichers, reporters).

**Почему отвергли**: headless API требует контроля над порядком стадий и над ответами для различных транспортов; адаптация стандартного Handler привела бы к сложности настройки и тестов.

---

## Consequences

### Положительные ✅
- Централизация конфигурации и логики ошибок в `ErrorKernel`.
- Строгий контракт (`ErrorPayload`) для всех потребителей (HTTP, jobs, CLI).
- Улучшенное логирование (trace-id, user-id, каналы).
- Лёгкая генерация артефактов (`docs/_generated/errors.md`).
- Упрощение unit/feature тестов за счёт явной фабрики и тестируемых стадий pipeline.

---

### Отрицательные ❌
- Требуется миграция всех существующих исключений и помощников на новый контракт.
- Разработка нового модуля увеличит начальные трудозатраты.
- Необходимо переобучение команды на новую архитектуру.

---

### Нейтральные ⚠️
- Временная поддержка двух систем до завершения миграции (feature toggle).
- Требуется обновление инструментов генерации документации (`docs:errors`).
- Возникает необходимость в дополнительных unit-тестах для `ErrorKernel`.

---

## Implementation Notes

- Файлы: `bootstrap/app.php`, `app/Support/Errors/*`, `config/errors.php`, `docs/40-architecture/error-handling.md`.
- Ввести Toggle `config('errors.kernel_enabled')` для поэтапного rollout.
- После миграции удалить `App\Support\Http\Problems`, `Problem`, `ProblemType`.
- Команда `composer docs:gen` должна вызывать обновлённый генератор `errors`.

---

## References

- [RFC 7807: Problem Details for HTTP APIs](https://www.rfc-editor.org/rfc/rfc7807)
- [docs/40-architecture/error-handling.md](../error-handling.md)
- [Laravel Exception Handling](https://laravel.com/docs/12.x/errors)
- [Google SRE Workbook — Handling Errors](https://sre.google/workbook/handling-errors/)

---

## History

| Date | Change | Author |
|------|--------|--------|
| 2025-11-10 | Created | @architect-team |

---

> 💡 **Usage Tips**  
> - Все новые обработчики регистрируйте через ErrorKernel.  
> - Для доменных исключений реализуйте `ErrorConvertible`.  
> - Поддерживайте документацию в `docs/_generated/errors.md`.

