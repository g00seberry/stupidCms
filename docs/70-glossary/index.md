---
owner: "@backend-team"
system_of_record: "narrative"
review_cycle_days: 90
last_reviewed: 2025-11-09
---

# Глоссарий

Определения ключевых терминов stupidCms.

## A

### ADR (Architecture Decision Record)

Документ, описывающий важное архитектурное решение с контекстом, опциями и последствиями.

См. [ADR Index](../40-architecture/adr/index.md)

### API Resource

Laravel класс для преобразования моделей в JSON-ответы API. Обеспечивает единообразный формат вывода.

### Audit Trail

Полный лог изменений данных: кто, что, когда изменил. Хранится в таблице `audits`.

## C

### Closure Table

Паттерн хранения иерархических данных. Используется в `term_tree` для быстрого доступа к предкам/потомкам.

### CORS (Cross-Origin Resource Sharing)

Механизм браузера для безопасных запросов между доменами. Настройки в `config/cors.php`.

### Cursor (AI Assistant)

IDE-ассистент с контекстом проекта. Читает `.cursorrules` и `/docs` для понимания кода.

## D

### Diátaxis

Фреймворк организации документации: Concepts, How-to, Reference, Architecture.

### Docs-as-code

Подход к документации: хранение в репозитории, версионирование, автогенерация из кода.

## E

### Entry

Запись контента (экземпляр PostType). Например, конкретная статья, страница или событие.

См. [Entries](../10-concepts/entries.md)

### EntrySlug

История URL записи. Позволяет автоматические 301-редиректы при изменении slug.

См. [Slugs & 301](../10-concepts/slugs.md)

### ERD (Entity-Relationship Diagram)

Диаграмма связей сущностей БД. Автогенерируется из миграций.

См. [ERD Reference](../30-reference/erd.md)

## F

### Form Request

Laravel класс для валидации HTTP-запросов. Используется в контроллерах для проверки данных.

### Frontmatter

YAML-метаданные в начале markdown-файлов. Содержит `owner`, `review_cycle_days`, `related_code`.

## H

### Headless CMS

CMS без жёстко связанного frontend. stupidCms предоставляет API для любых клиентов (React, Vue, Next.js, мобильные приложения). Встроенная админка на Blade — лишь один из возможных клиентов.

## J

### JWT (JSON Web Token)

Токен аутентификации. stupidCms использует JWT для API (HS256 или RS256).

См. [Security](../40-architecture/security.md)

## M

### Media Variant

Обработанная версия изображения (thumbnail, medium, large). Генерируется автоматически при загрузке.

## O

### Option

Настройка сайта в key-value формате. Например, `site_name`, `contact_email`.

См. [Options](../10-concepts/options.md)

### Outbox Pattern

Паттерн для надёжной отправки событий. Используется таблица `outbox` для transactional publishing.

## P

### Policy

Laravel класс для проверки прав доступа. Например, `EntryPolicy` определяет, кто может редактировать entry.

### PostType

Тип контента (article, page, event). Определяет поля, таксономии и настройки для entries.

См. [Post Types](../10-concepts/post-types.md)

### Publishing Flow

Жизненный цикл entry: Draft → Scheduled → Published → Archived.

## R

### Reserved Route

Системный URL, недоступный для пользовательских slugs. Например, `/api/*`, `/admin/*`.

### RFC7807

Стандарт форматирования ошибок API (Problem Details). Используется в stupidCms для единообразия.

## S

### Scope (Eloquent)

Переиспользуемый фильтр запросов. Например, `Entry::published()` возвращает только опубликованные записи.

### Seeder

Laravel класс для заполнения БД тестовыми данными. Используется в `php artisan db:seed`.

### Slug

URL-часть записи. Например, `my-article` в `/articles/my-article`.

См. [Slugs & 301](../10-concepts/slugs.md)

### Soft Delete

Логическое удаление записи (устанавливается `deleted_at`). Можно восстановить через `restore()`.

## T

### Taxonomy

Группа терминов для категоризации контента. Например, "Категории", "Теги", "Регионы".

См. [Taxonomy](../10-concepts/taxonomy.md)

### Term

Элемент таксономии. Например, конкретная категория "Laravel" в таксономии "Категории".

### TermTree

Иерархия терминов (parent-child). Реализовано через Closure Table.

## Сокращения

| Сокращение | Расшифровка                          |
| ---------- | ------------------------------------ |
| **ADR**    | Architecture Decision Record         |
| **API**    | Application Programming Interface    |
| **CORS**   | Cross-Origin Resource Sharing        |
| **CMS**    | Content Management System            |
| **CRUD**   | Create, Read, Update, Delete         |
| **ERD**    | Entity-Relationship Diagram          |
| **FK**     | Foreign Key                          |
| **JWT**    | JSON Web Token                       |
| **PK**     | Primary Key                          |
| **REST**   | Representational State Transfer      |
| **RFC**    | Request for Comments (стандарт IETF) |
| **SEO**    | Search Engine Optimization           |
| **SPA**    | Single Page Application (внешний фронтенд может быть SPA) |
| **SQL**    | Structured Query Language            |
| **URI**    | Uniform Resource Identifier          |
| **URL**    | Uniform Resource Locator             |

## Связанные страницы

-   [Quick Context](../00-start/quick-context.md) — быстрый обзор
-   [Concepts](../10-concepts/index.md) — подробные объяснения
-   [Domain Model](../10-concepts/domain-model.md) — схема данных

---

> 💡 **Не нашли термин?** Создайте issue или PR с предложением.
